[package]
name = "libraw-sys"
version = "0.1.1"
edition = "2024"
links = "raw"

[dependencies]
libc = "0.2"
libjpeg-turbo = { path = "../libjpeg-turbo" }

[build-dependencies]
vcpkg = "0.2"
cc = "1.0"
