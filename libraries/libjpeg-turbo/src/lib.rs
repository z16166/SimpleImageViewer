// FFI bindings for libjpeg-turbo (minimal for now as it's primarily used by LibRaw)
// In the future, we can add high-level Rust wrappers here for direct JPEG decoding.

pub fn link_test() {
    println!("libjpeg-turbo linked");
}
